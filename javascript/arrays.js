// an array is used to store values of similar or different data types in a single variable

// [] are used to create an array

var marks = [24, 34, 44, 3, 5, 66, 232];

// add a new item at the end of the list
marks.push(1000);
console.log(marks);

// removes last item from the list
marks.pop();
console.log(marks);
